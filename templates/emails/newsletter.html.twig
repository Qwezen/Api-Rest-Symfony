<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Sorties jeux vidéo de la semaine</title>
</head>
<body style="font-family: Arial, sans-serif; background: #f9f9f9; padding: 20px;">
    <h2> Les sorties jeux vidéo de la semaine</h2>

    {% if games is not empty %}
        <ul>
            {% for game in games %}
                <li>
                    <strong>{{ game.title }}</strong><br>
                    Sortie prévue le {{ game.releaseDate|date('d/m/Y') }}
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>Aucun jeu prévu cette semaine.</p>
    {% endif %}

    <hr>
    <p style="font-size: 12px; color: #888;">
        Vous recevez cet email car vous êtes abonné à notre newsletter.
    </p>
</body>
</html>
